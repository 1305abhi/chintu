import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF0B0B0F);
  static const surface = Color(0xFF12121A);

  static const primary = Color(0xFF6A5ACD);
  static const secondary = Color(0xFF00BFFF);

  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFA0A0B2);
}